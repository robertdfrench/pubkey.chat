name: Makefile CI

on:
  push:
    branches: [ "trunk" ]
  pull_request:
    branches: [ "trunk" ]

jobs:
  test-linux:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v4
    - name: Run Tests
      run: make test
  test-macos:
    runs-on: macos-12
    steps:
    - uses: actions/checkout@v4
    - name: Run Tests
      run: make test
